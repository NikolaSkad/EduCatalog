import React from 'react';
import ThemeListener from './ ThemeListener/ ThemeListener';
import AuthListener from './AuthListener/AuthListener';

const Listeners = () => {
  return (
    <>
      <ThemeListener />
      <AuthListener />
    </>
  );
};

export default Listeners;
